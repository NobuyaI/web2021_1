<!Doctype html>
<html>
<head>
  <meta charset="utf-8">
  <title>Show message</title>
</head>
<body>
  <h2>千葉県</h2>
  <table>
      <% for( let row of data ) { %>
        <tr>
            <td><%= row.id %></td>
            <td><%= row.市町村名 %></td>
            <td><%= row.人口 %></td>
          　<td><%= row.面積 %></td>
        </tr>
      <% } %>
  </table>
</body>
</html>